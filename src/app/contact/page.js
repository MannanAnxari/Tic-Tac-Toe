import React from 'react'

export const metadata = {
    title: 'Contact',
    description: 'Contact',
}

const page = () => {
    return (
        <div>
            asd
        </div>
    )
}

export default page
